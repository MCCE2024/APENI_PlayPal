# -------------------------------------------------------------------
# NESTJS/NODE.JS/TYPESCRIPT COMMON IGNORE RULES (Backend/NestJS)
# -------------------------------------------------------------------

# Dependency directories
/node_modules
# IGNORE node_modules IN ALL SUBDIRECTORIES, including services/servicename/node_modules
**/node_modules

/dist
/build
/lib
/es
/esm
/release

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# OS and Editor files
.DS_Store
.vscode/
.idea/
*.swp

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# TypeScript
# Compiled output (usually the /dist folder is enough, but listing all)
*.tsbuildinfo
*.js.map
*.d.ts

# Jest/Testing
/coverage
/e2e/

# NestJS specific
/test-results
# Do not commit your sqlite/other local database files if you are using one
*.sqlite
*.db

# -------------------------------------------------------------------
# NEXT.JS/REACT SPECIFIC IGNORE RULES (Frontend/Next.js)
# ---------------------------------------------------

# Next.js build output
.next/
out/

# Traditional React build output (might not be used in Next.js, but safe to include)
/public/build

# Image Optimization Cache (Next.js)
/public/images
/public/static/images

# Local development server and lock files
.npm
.eslintcache
.next-dev

# Storybook (if used)
.storybook/
/storybook-static/

# -------------------------------------------------------------------
# MISCELLANEOUS
# -------------------------------------------------------------------

# Temp files
*~
# Backup files
*.bak

### Terraform
# --------------------------------------------------------------------------
# Terraform / OpenTofu Specific Files
# --------------------------------------------------------------------------

# Local state file (always use a remote backend like S3, Azure Blob, etc.)
*.tfstate
*.tfstate.*
# The -backup suffix is used for prior state files after a successful apply, 
# but only when using a local backend.
*.tfstate.backup

# State Locking files (if using a local backend)
.terraform.tfstate.lock.info
.terraform-lock.hcl 

# The local directory where Terraform/OpenTofu downloads provider plugins 
# and modules. This should always be recreated by 'tofu init' or 'terraform init'.
.terraform/
.tofu/

# Crash/diagnostic logs
crash.log

# Secret/Sensitive files
# Generated credentials or output files that contain sensitive info
*.tfvars.json
# Often used for sensitive variable values, should not be committed
# Use environment variables or secure vault integrations instead.
secrets.tfvars

# Windows-specific files that sometimes sneak in
# (Note: On Linux/macOS, .terraformrc is usually global (~/.terraformrc) 
# and doesn't need to be ignored here unless you override it locally.)
terraform.rc

kubeconfig